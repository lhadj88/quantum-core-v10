# R23A2 temporary execution payload

This folder is a frozen, research-only execution payload for `R23A2_P2_TEMPORAL_BUDGET_VALIDATION_v0_1`.

It validates one partial mechanism inside a causal multi-process phenomenology. It does not search for a permanently dominant route and cannot authorize trading or a global directional classifier.
